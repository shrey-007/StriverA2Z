package Graphs.TopologicalSort;

public class CycleDetectionInDirectedGraph {
    // earlier we did it using dfs, where we marked visited array with 0(not visited),1(currently exploring), 2(already explored).
    // and ham usme backtracking krke visited update krte the. But bfs mai toh backtrack nhi kr skte toh agar poocha jaye ki ye question bfs se kese hoga
    // toh answer is kahn's algorithm
}
