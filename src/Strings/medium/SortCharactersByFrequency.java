package Strings.medium;

public class SortCharactersByFrequency {
    /**The intuition behind the solution is to use a HashMap to count the frequency of each character in the input
     * string. Then, a PriorityQueue (max heap) is used to sort characters based on their frequency in descending order.
     * Finally, a StringBuilder is used to build the result string by appending characters according to their frequency.
     */
}
